# Scheme Matching Agent

The BillSG Scheme Matching Agent. Given a user profile and optionally a parsed bill, this agent queries the billsg-policy MCP server for live Singapore healthcare scheme data, runs the deterministic eligibility matrix, cross-references the bill for missing subsidies, and returns a categorised list of schemes with action plans.
